FROM php:7.2-apache
WORKDIR /var/www/html

ARG BACKEND_ADDRESS
ENV BACKEND_ADDRESS=$BACKEND_ADDRESS

ARG BACKEND_PORT
ENV BACKEND_PORT=$BACKEND_PORT

RUN echo "backend address is $BACKEND_ADDRESS"

COPY index.php .
COPY bootstrap .